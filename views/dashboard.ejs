<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minimalistic Futuristic Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <!-- Latest compiled and minified CSS -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            color: #fff;
            font-family: 'Arial', sans-serif;
        }
    </style>
</head>

<body class="flex flex-col items-center mt-5 p-4 w-100">
    <section id="up"></section>
    <section id="down"></section>
    <section id="left"></section>
    <section id="right"></section>

    <div class="absolute h-full">
        <div class="w-full max-w-4xl ">
            <div class="text-2xl font-bold blog">My Blog</div>
            <div class="space-x-4 d-flex justify-between">
                <!-- <a href="#" class="text-gray-400 hover:text-white">Home</a> -->
                <!-- <a href="#" class="text-gray-400 hover:text-white">About</a> -->
                <a href="/addUpdateBlog" class="text-gray-400 hover:text-white bg-dark p-3 rounded-lg">Add Blogs</a>
                <!-- <a href="#" class="text-gray-400 hover:text-white">Contact</a> -->
                <a href="/logout" class="text-gray-400 hover:text-white text-end bg-dark p-3 rounded-lg">Logout</a>
            </div>
        </div>

        <main class="w-full max-w-4xl">
            <section class="mb-12">
                
            </section>

            <section class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% blogs.map((blog)=>{ %>
                    <article class="p-6 rounded-lg shadow-2xl border">
                        <img src="<%= blog.image %>" class="rounded-lg w-full h-40 " alt="">
                        <h2 class="text-2xl font-bold my-3 text-gray-900 fw-bold">
                            <%= blog.title %> 
                        </h2>
                       
                        <p class="text-gray-400 truncate">
                            <%= blog.content %>
                        </p>
                        <div class="flex justify-between">
                            <a href="/addUpdateBlog?id=<%= blog._id %>"
                                class="text-gray-100 hover:text-white mt-4 block bg-primary p-2  rounded-lg">Edit
                                blog</a>
                            <a href="/deleteBlog?id=<%= blog._id %>"
                                class="text-gray-100 hover:text-white mt-4 block bg-danger p-2 rounded-lg">Delete
                                blog</a>
                        </div>
                    </article>
                    <% }) %>

            </section>
        </main>

    
    </div>
    <!-- Latest compiled and minified JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>
</body>

</html>